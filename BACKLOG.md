# BACKLOG - ebpfkit
src/loader/ringbuf.rs:{287,341,375,376} | silent-fallback | low | Four `let _ = libc::close(...)` / `let _ = libc::munmap(...)` sites silently discarded the syscall return (EpollFd::drop close; RingbufConsumer::new producer-mmap-failure error path; RingbufConsumer::drop's two munmaps), so a failed unmap (leaked mapping / wrong len) or close error was invisible (Law 10). | FIXED (ONE-PLACE): added two helpers `munmap_or_warn(addr,len,context)` and `close_or_warn(fd,context)` that run the syscall and `eprintln!` loudly on a nonzero return (matching the crate's existing consume_ring_records eprintln diagnostics), then repointed all 4 sites to them with a descriptive context string. munmap failure now names the leaked mapping; close failure is logged (fd is still released on Linux even on error, so no retry). ebpfkit all targets green (40 lib + 33+33+... 0 failed, 0 warnings). NOTE: these 4 findings were originally misfiled in the ebpfsieve ledger (libs/performance/kernel/BACKLOG.md rows 247/302/336/337); recorded here in the correct per-crate ledger and those rows reconciled to point here. | status=done
src/loader/ringbuf.rs:329 | bug | critical | data_slice constructs slice from raw parts using unchecked, potentially out-of-bounds offset and len | FIXED: validation is enforced by `consume_ring_records` before calling `data_slice`; additionally `data_slice` is now an `unsafe fn` with a documented `SAFETY` contract that `offset + len` must fit in the double-mapped data region, and the call site is wrapped in `unsafe`. | status=done
src/loader/ringbuf.rs:209 | bug | high | integer overflow in round_record_len calculation when data_len is close to usize::MAX | FIXED: `total + 7` now uses `total.saturating_add(7)` so a near-usize::MAX data_len yields the largest 8-aligned value instead of wrapping/panicking in debug. Proving tests round_record_len_rounds_up_to_8 (0->8,1->16,8->16,9->24) + round_record_len_saturates_without_overflow (usize::MAX -> MAX&!7, no panic). | status=done
src/loader/ringbuf.rs:237 | bug | medium | epoll_wait returns error on EINTR signal instead of retrying | FIXED: `EpollFd::wait` now loops on `epoll_wait`; if the syscall returns -1 with `ErrorKind::Interrupted`, it retries. Other errors are returned immediately. Regression test `epoll_wait_retries_on_eintr` uses an `eventfd`, a `SIGUSR1` delivered while blocked, and asserts the wait still returns successfully after at least 15 ms. | status=done
src/compiler/verify.rs:36 | bug | medium | parse_character_class parses multi-byte UTF-8 sequences as invalid byte ranges and single bytes, leading to unexpected matching behavior | FIXED: parse_character_class now rejects valid UTF-8 input containing non-ASCII codepoints; raw invalid-UTF-8 bytes are accepted as byte-level ranges. Also rejects consecutive unescaped dashes as malformed. Regression tests: `character_class_rejects_non_ascii_codepoints`, `character_class_accepts_raw_invalid_utf8_bytes`, `character_class_rejects_consecutive_dashes`. | status=done
src/compiler/verify.rs:42 | capability | medium | character class parser does not resolve standard escape sequences like \n or \d, treating them as literal characters | FIXED: Added `decode_character_class_escape` with `Token` enum; decodes `\n`, `\r`, `\t`, `\\`, `\]`, `\[`, `\-`, `\.`, `\d`, `\s`, `\w` and rejects `\D`/`\S`/`\W` plus unknown escapes. Regression tests: `parse_rejects_empty_class_escapes`, `parse_range_requires_ordered_endpoints`, `character_class_decodes_standard_escapes`, `character_class_expands_class_escapes`, `character_class_rejects_negated_class_escapes`, `character_class_range_with_escape_endpoint_works`. | status=done
DONE 2026-07-14 | src/loader/prog.rs:41 | capability | medium | load_filter does not capture or return the BPF verifier log on failure, making compilation errors extremely hard to debug | allocate a log buffer and retry or capture log on EINVAL/error | FIXED: `load_filter` now allocates a 16 KiB verifier log buffer and sets `log_level`/`log_size`/`log_buf` in the `BpfAttrProgLoad` syscall struct. On failure it reads the buffer, trims trailing NULs, and appends the verifier log to the returned `std::io::Error` message so operators can diagnose BPF verifier rejections. Added tests `load_filter_includes_verifier_log_in_error` and `load_filter_rejects_empty_program`. `cargo +1.92.0 test -p ebpfkit` passes (53 lib + 33 unit + 1 doc, CARGO_EXIT=0). | status=done
src/compiler/codegen.rs:302 | dedup | medium | duplicate character class compilation functions compile_char_class and compile_character_class with inconsistent registers and interfaces | consolidate character class compilation into a single consistent API | status=done | FIXED: `compile_char_class` is now the ONE-PLACE code-generation path: it operates on `R7` (the canonical input byte register used by `compile_character_class` and `compile_literal_search`), emits `JLT + JLE` per `CharRange`, and updates its instruction estimate to `4 + 2*ranges`. `compile_character_class` is now a thin public wrapper that parses the bracket text into `PatternRange`s, converts them to `CharRange`s, and delegates to `compile_char_class` (with the existing empty-class fast-path). `verify.rs::estimate_character_class_instructions` was updated to match the new canonical length. `tests/unit/compiler.rs::run_char_class` now sets `R7` directly and no longer needs `BPF_LDX`. `cargo +1.92.0 test -p ebpfkit` passes (53 lib + 33 unit + 1 doc, CARGO_EXIT=0).
DONE 2026-07-14 src/compiler/codegen.rs:21 | bug | low | hardcoded pattern check rejects 256-byte pattern with incorrect maximum limit in error message | FIXED: the literal-search compile path already uses `super::MAX_BPF_PATTERN_LEN` (1024 bytes) and the `two_hundred_fifty_six_byte_identity_pattern_compiles` lib test passes; updated the stale `255-byte cap` comment/assertion in `tests/unit/compiler.rs` to reference `MAX_BPF_PATTERN_LEN`. Full `cargo +1.92.0 test -p ebpfkit` green (CARGO_EXIT=0). | status=done
src/loader/core_detect.rs:49 | silent-fallback | low | error reading or parsing /proc/sys/kernel/osrelease is silently swallowed and falls back to version 0.0.0 | propagate error or log warning instead of silent zero fallback | status=done | DONE 2026-07-14: already fixed in core_detect.rs. `kernel_version()` reads `/proc/sys/kernel/osrelease`, calls `parse_kernel_release`; on read or parse failure it invokes `warn_kernel_undetected` (once, via `std::sync::Once`) with the reason and falls back to `(0,0,0)` fail-closed. `parse_kernel_release` returns `None` for empty, non-digit, or overflowed major/minor/patch; tests `rejects_unparseable_major_so_caller_fails_closed` and `rejects_unparseable_minor_or_patch` cover the parse path. `cargo +1.92.0 test -p ebpfkit --lib` 51 passed (CARGO_EXIT=0). | status=done
tests/unit/core_detect_tests.rs:1 | stub | low | duplicate and unreferenced core_detect_tests.rs file in unit sub-directory | remove the unused duplicate file | status=done
tests/adversarial/break_it.rs:1 | stub | low | duplicate and unreferenced break_it.rs file in adversarial sub-directory | remove the unused duplicate file | status=done
src/loader/ringbuf.rs:130 | perf | low | page_size queries sysconf(_SC_PAGESIZE) on every call instead of caching it | FIXED: `page_size` now uses `static std::sync::OnceLock<usize>`; successful sysconf values are cached, errors return `std::io::Error::last_os_error()` and are not cached so later calls retry. | status=done
src/compiler/codegen.rs:33 | perf | low | compile_literal_search initializes Vec without capacity despite instruction count being statically predictable | FIXED: `compile_literal_search` now uses `Vec::with_capacity(literal_search_instruction_count(...))`; `literal_search_instruction_count` corrected to `4` for empty patterns and `14 + 2 * len` for non-empty patterns to match actual emitted `BpfInsn` length. Regression tests: `literal_count_for_empty_pattern_is_fixed`, `literal_count_matches_compiled_program_length`. | status=done